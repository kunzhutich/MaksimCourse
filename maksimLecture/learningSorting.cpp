#include <iostream>
#include <vector>

using namespace std;

class Sorting {
    // buble sort
    template<typename T>
    
    void bublesortT(std::vector<T>& arr) {
    }

    void bublesortInt(std::vector<int>& arr) {
    }

    void quickSortInt(std::vector<int>& arr) {
    }

    template<typename T>
    void bublesortT(std::vector<T>& arr) {
    }
};

class Containers {
    void learnVector() {
        auto v = vector<int>();
        v.push_back(2);
        v.push_back(3);
        v.push_back(3);
    }
    //  Dynamic Array (std::vector<T>) T must be have default and && move semantic
    //  Fix Array T[]
    //  BST Binary search tree (map<K, T>)
    //  Hashtable (unordered map<K, T>)
    //  Stack (T) stack<T>
    //  Pipe Queue (T) dequeue<T> 
    //  //Set (T) dequeue<T>
    //  Hashtable (unordered map<K, T>)
    //  Forward Link list
    //  Double Links List
    //  
    //  Complexity of indsert/delete/find... 
    //
};